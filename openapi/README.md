Coloque aqui a especificação oficial da API em `openapi.yaml` ou `openapi.json`.

Depois rode:

```bash
npm install
npm run generate:api
```

Este projeto foi preparado para não inventar endpoints. A conexão final dos contratos deve partir desta spec.
